# ===================================================================
# Spring Boot configuration to supplement the default test config
# but change the hostname for elasticsearch, to match the Gitlab
# runner environment
# ===================================================================

spring:
  jpa:
    properties:
      hibernate:
        search:
          backend:
            uris: http://elasticsearch:9200
